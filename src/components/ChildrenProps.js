const ChildrenProps = (props) => {
    const { title } = props;
    return (
        <div>
            <h1>{title}</h1>
            {/* {children} */}
        </div>
    )
}

export default ChildrenProps;